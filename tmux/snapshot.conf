# tmux theme — mountaineer-grey
# Palette mirrors examples/terminal-theme.toml and the airline theme.
# To switch themes, replace the values in this block only.

# ---- base16 palette (mountaineer-grey) ----
%hidden BG="#191919"          # ansi 0  / background      (airline gui00)
%hidden BG_ALT="#363636"      # bright0 / status fill     (airline gui01)
%hidden SEL="#2f4243"         # selection_bg / mid panel  (airline gui02)
%hidden FG_DIM="#b8b8b8"      #                           (airline gui04)
%hidden FG="#f0f0f0"          # foreground                (airline gui05)
%hidden RED="#AC8A8C"         # ansi 1
%hidden GREEN="#8AAC8B"       # ansi 2  — normal mode
%hidden YELLOW="#ACA98A"      # ansi 3
%hidden BLUE="#8F8AAC"        # ansi 4  — insert mode
%hidden MAGENTA="#AC8AAC"     # ansi 5  — visual mode
%hidden CYAN="#8AABAC"        # ansi 6  — replace mode

# ---- airline-style section roles ----
# Section A = mode/title (highlighted), B = middle, C = outer status fill.
%hidden A_FG="${BG}"
%hidden A_BG="${GREEN}"        # change to ${BLUE}/${MAGENTA}/${CYAN} to retheme
%hidden B_FG="${FG}"
%hidden B_BG="${SEL}"
%hidden C_FG="${FG_DIM}"
%hidden C_BG="${BG_ALT}"

set -g @left_sep ""
set -g @right_sep ""

# Pane borders
set -g pane-border-style "fg=${BG_ALT}"
set -g pane-active-border-style "fg=${A_BG}"
set -g pane-border-lines single
set -g pane-border-indicators colour

# Copy mode (visual selection)
set -g mode-style "bg=${SEL},fg=${FG}"

# Command/message line
set -g message-style "bg=${SEL},fg=${FG}"
set -g message-command-style "bg=${SEL},fg=${FG}"

set -g allow-rename on
set -g automatic-rename on

# Status bar - matches airline with Section A, B, C
set -g status "on"
set -g status-justify "left"
set -g status-style "bg=${C_BG}"
set -g status-left-style "none"
set -g status-right-style "none"
set -g status-right-length "250"
set -g status-left-length "100"
set -g status-interval 1

set -g  status-left "#[fg=${A_FG},bg=${A_BG},bold] #S #[fg=${A_BG},bg=${B_BG},nobold,nounderscore,noitalics]#{@left_sep}"
set -ag status-left "#[fg=${B_FG},bg=${B_BG}] #I:#P #[fg=${B_BG},bg=${C_BG},nobold,nounderscore,noitalics]#{@left_sep}"

# set -g  status-right "#[fg=${B_BG},bg=${C_BG}]#[fg=${C_FG},bg=${B_BG},bold] #(~/.scripts/tmux_timer.sh status) #[fg=${BG_ALT},bg=${B_BG}]"
set -g  status-right "#{session_dots} #[fg=${C_FG},bg=${C_BG},bold] #{now_playing} #[fg=${B_BG},bg=${C_BG}]#{@right_sep}"
set -ag status-right "#[fg=${B_FG},bg=${B_BG},bold] #(tmux-mem-cpu-load -m 2) #[fg=${A_BG},bg=${B_BG}]#{@right_sep}"
set -ag status-right "#[fg=${A_FG},bg=${A_BG},bold] %R "

# Inactive windows (Section C style)
set -g window-status-format "#[fg=${C_BG},bg=${C_BG},nobold,nounderscore,noitalics]#{@left_sep}#[default,fg=${C_FG},bg=${C_BG}] #W #[fg=${C_BG},bg=${C_BG}]#{@left_sep}"

# Active window (Section A style - highlighted)
set -g window-status-current-format "#[fg=${A_FG},bg=${A_BG},nobold,nounderscore,noitalics]#{@left_sep}#[fg=${A_FG},bg=${A_BG},bold] #W #[fg=${A_BG},bg=${C_BG},bold]#{@left_sep}"
